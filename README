1. 通过高德、百度或者谷歌等获取到石家庄地铁的数据。在我的案例中，我是通过高德获取的数据（数据见https://github.com/Arthur-lt/sjz-metro）
2. 处理数据，将数据里面的经纬度、站名提取出来，分别放在一个latlng&name的数组中
3. 新建3个layerGroup，用来存放1号线、3号线以及全部线路
4. 根据latlng数组，绘制站点和线路。用name数组给每个站点添加上名字
5. 设置js点击事件，根据用户点击线路显示对应的线路。注意：这里要使用removeLayer||removeFrom
